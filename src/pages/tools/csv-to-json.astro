---
import Layout from "@/layouts/Layout.astro";
import Header from "@/components/Header.astro";
import Footer from "@/components/Footer.astro";

const redirectUrl = "https://anyfreetools.com/tools/csv-to-json";
---

<Layout title="CSV 转 JSON - 跳转中 | 陈广亮的技术博客">
  <Header />
  <main class="app-layout flex min-h-[60vh] flex-col items-center justify-center text-center">
    <div class="max-w-md">
      <span class="text-6xl mb-6 block">📊</span>
      <h1 class="text-2xl font-bold mb-4">CSV 转 JSON 工具已迁移</h1>
      <p class="text-lg opacity-80 mb-6">
        此工具已迁移到独立工具站，3秒后自动跳转...
      </p>
      <a
        href={redirectUrl}
        class="inline-flex items-center gap-2 rounded-lg bg-accent px-6 py-3 font-medium text-white transition-all hover:opacity-90"
      >
        <span>立即跳转</span>
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M5 12h14" />
          <path d="M13 18l6-6" />
          <path d="M13 6l6 6" />
        </svg>
      </a>
      <p class="mt-6 text-sm opacity-60">
        或返回 <a href="/tools" class="text-accent hover:underline">工具列表</a>
      </p>
    </div>
  </main>
  <Footer />
</Layout>

<script define:vars={{ redirectUrl }}>
  // Auto redirect after 3 seconds
  setTimeout(() => {
    window.location.href = redirectUrl;
  }, 3000);
</script>
